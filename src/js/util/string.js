
define(function(require, exports) {
	exports.encodeHTML = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML2 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML3 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML4 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	}
	exports.encodeHTML3 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML4 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	}
	exports.encodeHTML3 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML4 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	}
	exports.encodeHTML5 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML6 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	}
	exports.encodeHTML7 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML8 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	}
	exports.encodeHTML9 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML10 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	}
	exports.encodeHTML11 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	};

	exports.encodeHTML12 = function(str) {
		if(typeof str == 'string'){
			var ar = ['&', '&amp;', '<', '&lt;', '>', '&gt;', '"', '&quot;'];
			for (var i = 0; i < ar.length; i += 2){
				str = str.replace(new RegExp(ar[i], 'g'), ar[1 + i]);
			}
			return str;
		}
		return str;
	}
});
